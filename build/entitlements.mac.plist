<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements para el hardened runtime del Parallax Editor firmado con
  Developer ID Application.

  El editor:
    • lanza subprocesos (`git`, `claude`) → `allow-unsigned-executable-memory`
      y `allow-jit` para que Electron pueda ejecutar V8/JIT dentro de un
      hardened runtime,
    • hace peticiones de red (npm, S3, GitHub API) → `network.client`,
    • usa el Keychain via safeStorage → `keychain-access-groups` implícito,
    • lee/escribe carpetas que el usuario elige por dialog nativo →
      `user-selected.read-write`,
    • NO requiere cámara, micrófono, ubicación ni Apple Events cross-app.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
